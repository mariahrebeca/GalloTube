@model GalloTube.Models.Video
@{
    ViewData["Title"] = "Home";
}

<div class="album py-5">
    <div class="text-center mb-3">
        <h1>@Model.Name</h1>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <img src="@Model.VideoFile" alt="@Model.Name" class="img-fluid">
            </div>
            <div class="col-md-9">
        
                <p>Duração: @Model.Duration</p>
                @foreach(VideoTag VideoTag in Model.Tags)
                {
                    <span class="badge bg-secondary me-2">@VideoTag.Tag.Name</span>
                }
            </div>
        </div>
    </div>
</div>